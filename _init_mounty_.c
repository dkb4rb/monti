#include "monty.h"

int run_mounty(char *fd_scriptor);

int run_mounty(char *fd_scriptor)
{
    printf("Running Mounty\n");
    if (fd_scriptor == NULL)
        printf("NO hay un FD");

    return (0);
}